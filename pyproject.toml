[project]
name = "i18n-portfolio"
version = "0.1.0"
description = "An anonymized portfolio demo for an AI-assisted i18n delivery workflow."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
